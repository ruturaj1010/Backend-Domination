<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>All Posts</title>
    <link rel="stylesheet" href="style.css">
</head>
<body>
    <h1 class="head">Quora Posts</h1>

    <div class="main">
        <% for (post of posts) { %>
            <div class="post">
                <h3 class="username"> @<%= post.username %> <h3>
                <p> <%= post.content %></p>

                <div class="links">
                    <a href="http://localhost:8080/posts/<%=post.id%>">View in details</a>
                    <a href="http://localhost:8080/posts/<%=post.id%>/edit">Edit post</a>
                </div>

                <form action="/posts/<%=post.id%>?_method=DELETE" method="post">
                    <button class="delBtn">Delete post</button>
                </form>
            </div>
        <% } %>

    </div>
        
    <a class="newpostBtn" href="http://localhost:8080/posts/new">Create New Post</a>

</body>
</html>